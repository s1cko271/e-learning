# ============================================
# Production Configuration
# Sử dụng Environment Variables từ Render
# ============================================

# MySQL Database Configuration (từ Aiven)
# Các giá trị này sẽ được override bởi Environment Variables trên Render
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/coursemgmt_test?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false

# Server Configuration
server.port=${PORT:8080}

# CORS Configuration (cho phép Vercel frontend)
spring.web.cors.allowed-origins=${ALLOWED_ORIGINS:https://your-frontend.vercel.app,http://localhost:3000}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# VNPay Configuration
vnpay.url=${VNPAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.return-url=${VNPAY_RETURN_URL:https://your-frontend.vercel.app/payment/vnpay-return}
vnpay.ipn-url=${VNPAY_IPN_URL:https://your-backend.onrender.com/api/v1/vnpay/ipn}
vnpay.tmn-code=${VNPAY_TMN_CODE:YOUR_TMN_CODE}
vnpay.hash-secret=${VNPAY_HASH_SECRET:YOUR_HASH_SECRET}

# File Storage (sử dụng absolute path trên Render)
certificate.storage.path=${CERTIFICATE_STORAGE_PATH:/app/certificates}
certificate.base-url=${CERTIFICATE_BASE_URL:https://your-backend.onrender.com/certificates}

avatar.storage.path=${AVATAR_STORAGE_PATH:/app/uploads/avatars}
avatar.base-url=${AVATAR_BASE_URL:https://your-backend.onrender.com/api/files/avatars}

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.coursemgmt=INFO
logging.level.org.springframework.web=WARN
logging.file.name=logs/application.log

# JWT Configuration
coursemgmt.app.jwtSecret=${JWT_SECRET:your-super-secret-jwt-key-change-this-in-production}
coursemgmt.app.jwtExpirationMs=86400000

# Mail Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Google Gemini API Configuration
gemini.api.key=${GEMINI_API_KEY:your-gemini-api-key}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1beta}
gemini.api.model=${GEMINI_API_MODEL:gemini-2.5-flash}

